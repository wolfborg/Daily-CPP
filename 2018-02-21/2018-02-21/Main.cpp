#include <iostream>
#include <string>
using namespace std;

int encode(string str, int key);

int main() {
	encode("scout",1939);
	encode("masterpiece", 1939);
}

int encode(string str, int key) {
	return 0;
}